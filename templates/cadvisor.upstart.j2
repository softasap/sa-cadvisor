description "Google CAdvisor"

start on runlevel [2345]
stop on runlevel [016]

respawn
console log

exec start-stop-daemon --chdir ${HOME} --start --make-pidfile --pid=/var/run/cadvisor.pid --exec {{cadvisor_install_dir}}/cadvisor -- --port {{ cadvisor_port | default(9280) }}

